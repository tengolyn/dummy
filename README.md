# Indic-Runner

A model- and hardware-agnostic execution framework for Indic AI workloads (reasoning, summarization, translation, OCR).

See `docs/architecture.md` for the full design and `CLAUDE.md` for a working summary of the architecture, dataset schemas, and output format. Source lives under `backend/`.
